# 复制本文件为 .env 并填写实际值（项目只读取 .env，不会读 .env.example）
# 例如：cp .env.example .env  或  copy .env.example .env

# 数据库
DB_HOST=127.0.0.1
DB_NAME=ctlp
DB_USER=root
DB_PASS=
DB_PORT=3306

# SMTP（可直接使用）
SMTP_HOST='smtp.163.com'
SMTP_PORT=465
SMTP_SECURE=ssl
SMTP_USER=vechisss@163.com
SMTP_PASS=HUeCcXHtxDeaZNmn
SMTP_FROM=vechisss@163.com
SMTP_FROM_NAME=CTLP
